
<header class="main-header">
    <a ui-sref="home" class="logo bg-light-blue">
        <span class="logo-lg">Fleet Management</span>
    </a><!-- e-Procurement PT. Asuransi Jiwasraya (Persero) -->
    <!-- Header Navbar: style can be found in header.less -->
    <nav class="navbar navbar-static-top bg-light-blue">
        <!-- Sidebar toggle button-->
        <a class="sidebar-toggle" ng-click="tmCtrl.toggle()" data-toggle="offcanvas" role="button">
            <span class="sr-only">Toggle navigation</span>
        </a>
        <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">
                <!-- User Account: style can be found in dropdown.less -->
                <li>
                    <a style="cursor: none; font-family: courier;">
                        <!--<div x-clock></div>-->
                        {{$root.serversekarang}}
                    </a>
                </li>
                <li class="dropdown notifications-menu">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{main.currTime}}
                    </a>
                </li>
                <!--<li class="dropdown notifications-menu">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{main.getCurrLang().toString().toUpperCase()}}
                    </a>
                    <ul class="dropdown-menu">
                        <li ng-repeat="lang in main.getLangList()">
                            <a ng-click="main.setLang(lang.id)">{{lang.bahasa}}</a>
                        </li>
                    </ul>
                </li>
                <li class="dropdown notifications-menu">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        <i class="fa fa-bell-o"></i>
                        <span class="label label-warning">{{ main.jumlahRequestAktivasi + main.jumlahRequestVerifikasi + main.jumlahRequestUbahData + $root.jumlahExpired}}</span>
                    </a>
                    <ul class="dropdown-menu">
                        <li class="header">Anda memiliki {{ main.jumlahRequestAktivasi + main.jumlahRequestVerifikasi + main.jumlahRequestUbahData + $root.jumlahExpired}} jumlah pemberitahuan!</li>
                        <li>-->
                <!-- inner menu: contains the actual data -->
                <!--<ul class="menu">
                    <li ng-if="main.jumlahRequestAktivasi !== 0 && (main.currRoles.indexOf('APPLICATION.ROLE_ADMINPROC') !== -1 || main.currRoles.indexOf('APPLICATION.ROLE_MGRPROC') !== -1)">
                        <a ng-click="main.menujuAktivasi()" style="cursor: pointer;">
                            <i class="fa fa-check text-blue"></i> <strong>{{main.jumlahRequestAktivasi}}</strong> Pendaftar yang belum teraktivasi
                        </a>
                    </li>
                    <li ng-if="main.jumlahRequestVerifikasi !== 0 && (main.currRoles.indexOf('APPLICATION.ROLE_ADMINPROC') !== -1 || main.currRoles.indexOf('APPLICATION.ROLE_MGRPROC') !== -1)">
                        <a ng-click="main.menujuAktivasi()" style="cursor: pointer;">
                            <i class="fa fa-check-square text-green"></i> <strong>{{main.jumlahRequestVerifikasi}}</strong> Calon rekanan belum terverifikasi
                        </a>
                    </li>
                    <li ng-if="main.jumlahRequestUbahData !== 0 && (main.currRoles.indexOf('APPLICATION.ROLE_ADMINPROC') !== -1 || main.currRoles.indexOf('APPLICATION.ROLE_MGRPROC') !== -1)">
                        <a ng-click="main.toChangeRequest()" style="cursor: pointer;">
                            <i class="fa fa-comments text-red"></i> <strong>{{main.jumlahRequestUbahData}}</strong> Permintaan ubah data
                        </a>
                    </li>-->
                <!--<li ng-if="!($root.jumlahExpired === 0)" style="cursor: pointer;">
                    <a ng-click="menujuExpired()">
                        <i class="fa fa-warning text-yellow"></i> <strong>{{$root.jumlahExpired}}</strong> Rekanan yang dokumennya expired
                    </a>
                </li>-->
                <!--<li ng-if="!(main.rfqvhsApprvReq === 0)" style="cursor: pointer;">
                    <a ng-click="toRFQVHSApproval()">
                        <i class="fa fa-check text-blue"></i> <strong>{{main.rfqvhsApprovalRequestCount}}</strong> Approval VHS / FPA
                    </a>
                </li>-->
                <!--<li ng-if="!(main.contractReqApprvReq === 0)" style="cursor: pointer;">
                    <a ng-click="toContractReqApprv()">
                        <i class="fa fa-check text-blue"></i> <strong>{{main.contractReqApprvReqCount}}</strong> Approval Contract Requisition
                    </a>
                </li>-->
                <!--</ul>
                        </li>
                    </ul>
                </li>-->

                <li class="dropdown user user-menu">
                    <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="glyphicon glyphicon-user"></i>
                        <span class="afta-font">
                            @User.Identity.Name
                            <i class="caret"></i>
                        </span>
                    </a>
                    <ul class="dropdown-menu">
                        <!-- Menu Footer-->
                        <li class="user-footer">
                            <div class="pull-left">
                                <a ui-sref="view-akun-user" class="btn btn-default btn-flat">Akunku</a>
                            </div>
                            <div class="pull-right">
                                <a ng-click="main.logout()" class="btn btn-default btn-flat">Keluar</a>
                                <!--<a ui-sref="login-panitia" class="btn btn-default btn-flat">Keluar</a>-->
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </nav>
</header>
