@model FMS.Website.Models.CsfItemModel

<table id="tbCsfVehicle" class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Manufacturer</th>
            <th>Model</th>
            <th>Series</th>
            <th>Body Type</th>
            <th>Vendor</th>
            <th>Color</th>
            <th>Start Period</th>
            <th>End Period</th>
        </tr>
    </thead>
    <tbody id="tb-body-select-vehicle">
        @if (Model.Detail.Models != null)
        {
            <tr>
                <td>@Html.Raw(Model.Detail.Manufacturer)</td>
                <td>@Html.Raw(Model.Detail.Models)</td>
                <td>@Html.Raw(Model.Detail.Series)</td>
                <td>@Html.Raw(Model.Detail.BodyType)</td>
                <td>@Html.Raw(Model.Detail.VendorName)</td>
                <td>@Html.Raw(Model.Detail.Color)</td>
                <td>@Html.Raw(Model.Detail.StartPeriod.ToString("dd-MMM-yyyy hh:mm:ss"))</td>
                <td>@Html.Raw(Model.Detail.EndPeriod.ToString("dd-MMM-yyyy hh:mm:ss"))</td>
            </tr>
        }
    </tbody>
</table>